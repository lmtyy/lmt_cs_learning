# 动态规划总结（p119-124）

本章最后对动态规划进行全面总结：基本思想、特点、思路、适用条件、应用场景和设计步骤。

---

## 一、动态规划的基本思想

将问题分解为若干小问题，解子问题，然后从子问题得到原问题的解。

---

## 二、动态规划特点

将问题分解为子问题，这些子问题往往**不相互独立**。

> 如果可以用分治法求解，分解的子问题太多，因此用分治法时间代价太高，消耗指数时间。

| | 分治法 | 动态规划 |
|--|--------|---------|
| 子问题关系 | 相互独立 | 不相互独立（有重叠） |
| 同一子问题 | 只出现一次 | 反复出现多次 |
| 时间代价 | 多项式级（无冗余） | 若用分治则指数级（冗余） |

---

## 三、动态规划思路

- 如果能够保存已解决的子问题的答案，而在需要时再找出已求得的答案，这样就可以**避免大量的重复计算**，节省时间。
- 动态规划方法用一个**表**来记录所有已解的子问题的答案。不管该子问题以后是否被用到，只要它被计算过，就将其结果填入表中。
- 这就是动态规划法的基本思路。具体的动态规划算法多种多样，但它们具有**相同的填表格式**。

> ✅ 核心：**用空间换时间**，表驱动（table-driven）。

---

## 四、动态规划问题的特征

动态规划算法的有效性依赖于问题本身所具有的两个重要性质：

### 1. 最优子结构（Optimal Substructure）

当问题的最优解包含了其子问题的最优解时，称该问题具有最优子结构性质。

> 保证了可以从子问题的最优解推出原问题的最优解。

### 2. 重叠子问题（Overlapping Subproblems）

在用递归算法自顶向下解问题时，每次产生的子问题并不总是新问题，有些子问题被反复计算多次。动态规划算法正是利用了这种子问题的重叠性质，对每一个子问题只解一次，而后将其解保存在一个表格中，在以后尽可能多地利用这些子问题的解。

> 保证了用表存储能显著减少计算量。

### 两者缺一不可

| 缺少哪个 | 后果 |
|---------|------|
| 缺少最优子结构 | 子问题最优 ≠ 原问题最优，DP 不适用 |
| 缺少重叠子问题 | 子问题不重复，直接分治即可，不需要存表 |

---

## 五、应用

往往求解具有某种**最优性质**的问题，此类问题往往具有多个解，我们要找到具有**最优值**的那个解。

典型应用场景：
- 最少/最多（计算量、代价、价值）
- 最优排列/组合（调度顺序、加括号方式）
- 最优结构（搜索树结构）

### 本章5个经典问题

| 问题 | 优化目标 | 时间复杂度 |
|------|---------|-----------|
| 矩阵连乘 | 最少乘法次数 | O(n³) |
| 流水作业调度 | 最短完工时间 | O(n log n)* |
| 0-1 背包 | 最大价值 | O(nc) |
| 最优二叉搜索树 | 最小平均搜索代价 | O(n³) |
| 备忘录方法 | — | 与 DP 同量级 |

*流水作业调度借助 Johnson 法则简化为排序问题。

---

## 六、设计动态规划法的步骤

| 步骤 | 内容 | 关键产出 |
|------|------|---------|
| **1** | 找出最优解的性质，并刻画其**结构特征** | 最优子结构证明 |
| **2** | 递归地定义最优值（写出**动态规划方程**） | 递推公式 |
| **3** | 以**自底向上**的方式计算出最优值 | 填表代码 |
| **4** | 根据计算最优值时得到的信息，**构造一个最优解** | Traceback |

### 补充说明

- 步骤 1-3 是动态规划算法的**基本步骤**
- 在只需要求出最优值的情形，步骤 4 可以省略，步骤 3 中记录的信息也较少
- 若需要求出问题的一个最优解，则必须执行步骤 4，步骤 3 中记录的信息必须足够多以便构造最优解

---

## 全章知识结构图

```
┌─────────────────────────────────────────────────────────┐
│                  Chapter 11: 动态规划                      │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  核心思想：分解 → 存储 → 避免重复                          │
│  两大性质：最优子结构 + 重叠子问题                         │
│  标准四步：结构 → 递推 → 填表 → 回溯                      │
│                                                         │
├──────────────┬──────────────┬───────────────────────────┤
│  区间DP       │  线性DP       │  特殊问题                  │
│  矩阵连乘     │  0-1背包      │  流水作业调度               │
│  最优BST      │              │  (Johnson法则)             │
├──────────────┴──────────────┴───────────────────────────┤
│  实现策略对比：                                            │
│  ┌──────────┬──────────┬──────────┐                     │
│  │ 朴素递归  │ 备忘录    │ 自底向上  │                     │
│  │ 指数级    │ 多项式级  │ 多项式级  │                     │
│  │ 不存表    │ 按需存表  │ 全部填表  │                     │
│  └──────────┴──────────┴──────────┘                     │
└─────────────────────────────────────────────────────────┘
```
